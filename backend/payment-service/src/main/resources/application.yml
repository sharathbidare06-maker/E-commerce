server:
  port: 8085
spring:
  application:
    name: payment-service
  datasource:
    url: jdbc:postgresql://localhost:5437/payment_db
    username: ecommerce
    password: ecommerce
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
